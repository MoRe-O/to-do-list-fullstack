PORT=3003
JWT_SECRET=replace_this_with_a_long_random_string
OTP_EXPIRY_MINUTES=5

# Optional — leave these blank to just log OTP codes to the server console
# instead of emailing them (useful while developing locally).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
